<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zktr.yuyi.dao.hedangren.SysUserRoleDao">

    <resultMap type="com.zktr.yuyi.entity.hedangren.SysUserRole" id="SysUserRoleMap">
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="roleId" column="role_id" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="SysUserRoleMap">
        select user_id,
               role_id
        from jdboot.sys_user_role
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="SysUserRoleMap">
        select user_id,
               role_id
        from jdboot.sys_user_role limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="SysUserRoleMap">
        select
        user_id, role_id
        from jdboot.sys_user_role
        <where>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="roleId != null">
                and role_id = #{roleId}
            </if>
        </where>
    </select>
    <select id="findtfhasuser" resultMap="SysUserRoleMap" >
        select * from sys_user_role where role_id=#{roleId}
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="" useGeneratedKeys="true">
        insert into jdboot.sys_user_role(user_id, role_id)
        values (#{userId}, #{roleId})
    </insert>

    <insert id="insertBatch" keyProperty="" useGeneratedKeys="true">
        insert into jdboot.sys_user_role(user_id, role_id)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userId}, #{entity.roleId})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="" useGeneratedKeys="true">
        insert into jdboot.sys_user_role(user_id, role_id)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userId}, #{entity.roleId})
        </foreach>
        on duplicate key update
        user_id = values(user_id) , role_id = values(role_id)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update jdboot.sys_user_role
        <set>
            <if test="userId != null">
                user_id = #{userId},
            </if>
            <if test="roleId != null">
                role_id = #{roleId},
            </if>
        </set>
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from jdboot.sys_user_role
    </delete>

</mapper>

